# ===========================================
# Genshin Story QA System - 环境变量配置
# ===========================================
# 复制此文件为 .env 并填入实际值
# 注意：.env 文件不应提交到版本控制

# -------------------------------------------
# LLM Provider (选择一个)
# -------------------------------------------

# Google Gemini (Primary for Agent)
# API Key: 支持 GOOGLE_API_KEY 或 GEMINI_API_KEY
GEMINI_API_KEY=your_gemini_api_key  # 别名

# LLM Model: 支持 LLM_MODEL 或 GEMINI_MODEL
GEMINI_MODEL=xxx  # 别名
GRADER_MODEL=xxx
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/

# Grader/Refiner 模型 (可选，默认 gemini-2.5-flash)
# 评分和查询分解使用轻量模型以提高速度
# GRADER_MODEL=gemini-2.5-flash

# Anthropic Claude (Fallback)
# ANTHROPIC_API_KEY=sk-ant-xxxxx

# OpenAI GPT-4
# OPENAI_API_KEY=sk-xxxxx

# 阿里云 Qwen (可选)
# DASHSCOPE_API_KEY=sk-xxxxx

# -------------------------------------------
# Embedding Model
# -------------------------------------------

# 使用本地模型时无需配置
# 如使用 OpenAI embedding:
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# 本地嵌入模型路径 (可选，默认自动下载)
# LOCAL_EMBEDDING_MODEL=BAAI/bge-base-zh-v1.5

# -------------------------------------------
# Vector Database - Qdrant
# -------------------------------------------

QDRANT_HOST=localhost
QDRANT_PORT=6333
# 如使用 Qdrant Cloud:
# QDRANT_URL=https://xxx.qdrant.io
# QDRANT_API_KEY=xxxxx

# -------------------------------------------
# Graph Database - Neo4j
# -------------------------------------------

NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password

# -------------------------------------------
# Application Settings
# -------------------------------------------

# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 数据目录
DATA_DIR=./Data

# 索引存储目录
INDEX_DIR=./index_storage
