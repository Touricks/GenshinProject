# ===========================================
# Genshin Story QA System - 环境变量配置
# ===========================================
# 复制此文件为 .env 并填入实际值
# 注意：.env 文件不应提交到版本控制

# -------------------------------------------
# LLM Provider
# -------------------------------------------

# Google Gemini API Key
# 支持 GOOGLE_API_KEY 或 GEMINI_API_KEY
GEMINI_API_KEY=your_gemini_api_key
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/

# -------------------------------------------
# Model Configuration (三类模型)
# -------------------------------------------

# REASONING_MODEL: 主 Agent 推理、工具编排
# 需要强推理能力，适合复杂的多步骤任务
REASONING_MODEL=gemini-2.5-flash

# GRADER_MODEL: 答案质量评分、Query 改写
# 使用快速模型以提高响应速度
GRADER_MODEL=gemini-2.5-flash

# DATA_MODEL: 结构化输出 (KG 提取、Event 提取)
# 适合生成 JSON 格式的结构化数据
DATA_MODEL=gemini-2.5-flash

# -------------------------------------------
# Fallback Providers (可选)
# -------------------------------------------

# Anthropic Claude
# ANTHROPIC_API_KEY=sk-ant-xxxxx

# OpenAI GPT-4
# OPENAI_API_KEY=sk-xxxxx

# 阿里云 Qwen
# DASHSCOPE_API_KEY=sk-xxxxx

# -------------------------------------------
# Embedding Model
# -------------------------------------------

# 使用本地模型时无需配置
# 如使用 OpenAI embedding:
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# 本地嵌入模型路径 (可选，默认自动下载)
# LOCAL_EMBEDDING_MODEL=BAAI/bge-base-zh-v1.5

# -------------------------------------------
# Vector Database - Qdrant
# -------------------------------------------

QDRANT_HOST=localhost
QDRANT_PORT=6333
# 如使用 Qdrant Cloud:
# QDRANT_URL=https://xxx.qdrant.io
# QDRANT_API_KEY=xxxxx

# -------------------------------------------
# Graph Database - Neo4j
# -------------------------------------------

NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password

# -------------------------------------------
# Application Settings
# -------------------------------------------

# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 数据目录
DATA_DIR=./Data

# 索引存储目录
INDEX_DIR=./index_storage
